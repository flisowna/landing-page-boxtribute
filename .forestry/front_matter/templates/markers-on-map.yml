---
label: markers on map
hide_body: false
fields:
- name: details_of_the_markers
  type: field_group_list
  fields:
  - name: name_place
    type: text
    config:
      required: false
    label: name of the place
  - name: name_organisation
    type: text
    config:
      required: false
    label: name of the organisation
  - name: description_project
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: description of the project
  - name: gps_coordinates
    type: number
    label: GPS coordinates of the place
    description: 'to get the coordinates hold mouse/finger in the google maps on the
      place you want to add and copy two numbers, an example: 51.989464, 13.218106<br>'
    default: 0
    required: true
    config:
      min: 
      max: 
      step: 
  config:
    min: 
    max: 
    labelField: 
  label: details of the markers
  description: a form for placing a new marker on the map
